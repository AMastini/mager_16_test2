use strict';
odoo.define('custom.delivery.amount', function (require) {
require('web.dom_ready');
var ajax = require('web.ajax');
var onButton = function(e) { console.log(e);
}
button.click(_onButton);
});